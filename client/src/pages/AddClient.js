import AddClientForm from "../components/AddClientForm";

const AddClient = () => {
  return (
    <div>
      <h2>Create New client</h2>
      <AddClientForm />
    </div>
  );
};

export default AddClient;
